// var wordArray = [];

// $('#words span').each(function(index, element){
//     wordArray.push($(element).html());
// });

// copy(wordArray);
module.exports = {
    list: function() {
        return //wordlist
    }
}